STATIC_ROOT = "{{ code_root }}static/"

MEDIA_URL = "/media/"
MEDIA_ROOT = "{{ code_root }}media/"

ALLOWED_HOSTS = ['*']

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ database_user }}',
        'USER': '{{ database_user }}',
        'PASSWORD': '{{ database_pass }}',
        'HOST': 'localhost',
        'PORT': '',
    }
}
